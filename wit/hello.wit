package yeseh:wasm-research;

world hello {
    // Ask the host for the name of the implementation 
    import get-name: func() -> string;
    // Ask the host to print a message 
    import print: func(msg: string);
    // Export the run method
    export hello: func();
}


